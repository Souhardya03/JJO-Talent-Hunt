version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Install pnpm globally before running install
        - export NODE_OPTIONS="--max-old-space-size=6144"
        - npm ci
        - npm install -g pnpm
        - pnpm install
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*